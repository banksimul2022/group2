const express = require('express');
const router = express.Router();
const lukitus = require('../models/lukitus_model');

router.get('/:Kortinnumero?',
 function(request, response) {
  if (request.params.Kortinnumer) {
    lukitus.getPin(request.params.Kortinnumero, function(err, dbResult) {
      if (err) {
        response.json(err);
      } else {
        response.json(dbResult);
      }
    });
  } else {
    lukitus.getAll(function(err, dbResult) {
      if (err) {
        response.json(err);
      } else {
        response.json(dbResult);
      }
    });
  }
});


router.post('/', 
function(request, response) {
  lukitus.add(request.body, function(err, dbResult) {
    if (err) {
      response.json(err);
    } else {
      response.json(request.body);
    }
  });
});


router.delete('/:id', 
function(request, response) {
  lukitus.delete(request.params.id, function(err, dbResult) {
    if (err) {
      response.json(err);
    } else {
      response.json(dbResult);
    }
  });
});


router.put('/:Kortinnumero', 
function(request, response) {
  lukitus.update(request.params.Kortinnumero, request.body, function(err, dbResult) {
    if (err) {
      response.json(err);
    } else {
      response.json(dbResult);
    }
  });
});

module.exports = router;